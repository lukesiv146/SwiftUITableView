import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image("jordan")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Jordan Love!")
            Text("Bears")
            
        }
    }
}
